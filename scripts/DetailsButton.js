export const detailsSideButton = () => {
  return `<button>Details</button>`;
};

// We firstly need a way to make sure this doesn't run automatically. We are also going to need an onclick function that should be declared in this module.
